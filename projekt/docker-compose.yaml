version: "3.9"

services:
  server:
    build:
      context: ./server
    container_name: psi-server
    command: python3 server.py 1234 10
    ports:
      - "1234:1234"
    networks:
      - psi-net
    tty: true
    stdin_open: true

  client:
    build:
      context: ./client
    container_name: psi-client
    command: python3 client.py server 1234
    depends_on:
      - server
    networks:
      - psi-net
    tty: true
    stdin_open: true

networks:
  psi-net:
    driver: bridge
